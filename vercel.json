{
  "buildCommand": "echo Backend API - no build step",
  "outputDirectory": ".",
  "installCommand": "cd api && npm install && cd ../server && npm install",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ],
  "functions": {
    "api/index.js": {
      "maxDuration": 30,
      "includeFiles": "server/**"
    }
  }
}
